# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (never commit real secrets; examples are tracked)
.env
.env.*
!.env.example
!**/.env.example

# Python virtual environments
.venv/
venv/
env/

# Python bytecode and tool caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# SQLite databases and backup/journal variants (operational + visitor data)
*.db
*.db-journal
*.db.old
*.db.bak
*.sqlite
*.sqlite3
*.db3

# Generated application artifacts / uploaded visitor data
backend/uploads/
backend/logs/
print-agent/downloaded-badges/
downloaded-badges/

# Live runtime system settings — copy from system_settings.template.json per install.
# The template is tracked; the live file is site-specific and runtime-mutable.
backend/config/system_settings.json

# User-created themes — runtime-mutable, created by the Theme Editor.
backend/config/user_themes.json

# Generated repository inventory snapshot (regenerate on demand; do not track)
repo_files.txt

# Internal docs
docs/reviews