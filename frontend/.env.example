# PBC Visitor Kiosk — frontend environment (EXAMPLE)
#
# Copy this file to `frontend/.env` and set the backend URL for your network.
# The real `.env` is git-ignored and MUST NOT be committed.

# Base URL of the backend API (host/IP reachable from the kiosk browser).
VITE_API_BASE=http://your-backend-host:8000